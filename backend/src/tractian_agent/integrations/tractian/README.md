# Integração TRACTIAN

Cliente HTTP e tools tipadas para os 18 endpoints da API entregue. Toda leitura preserva o
envelope `{mode, data, notes}` e toda mutação aplica as proteções definidas no domínio.
