# Grafo

Estado compartilhado, nós, subgrafos, reducers e roteamento do LangGraph. Gates
determinísticos e limites de repetição também são conectados aqui, mas suas regras de
domínio ficam em `domain/`.
